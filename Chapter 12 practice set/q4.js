//Write a simple interest calculator using javascript.

let p=prompt("Enter the principle:");
let i= prompt("Enter the interest rate.");
let t = prompt("Enter the time:");

si=(p*t*i)/100;

console.log(`The simple interst on ${p} with interest ${i} on time ${t} is ${si}`);

// prompt("Ask me anything?");